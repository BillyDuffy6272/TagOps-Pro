# 09 - Iteration Log

> **Status: template, not yet written.** Slotted for Week 8 (IU12SE-018/019). A running log of user acceptance testing (UAT) feedback and deployment iteration — what real usage (yours, a friend's, the teacher's) turned up, and what changed as a result. Add new entries at the bottom, oldest first, like `ai-use-log.md`.
>
> **Naming note:** `CLAUDE.md`'s required repo shape names this file `09-iteration-log.md`; the file that actually exists is `09-iteration.md`. Worth a one-line `decision-log.md` entry either confirming the shorter name deliberately or renaming to match — small thing, but a marker checking the folio against the brief will notice the mismatch.

---

## UAT feedback log

*One entry per testing session. Who tested, what they tried, what they said or where they got stuck, and what you actually did about it (fixed / deferred / rejected, and why).*

| Date | Tester | What they tried | Feedback | Action taken |
|---|---|---|---|---|
| | | | | |

## Deployment iteration log

*One entry per notable deploy — a Vercel build failure and its fix, an environment-variable misconfiguration caught after deploying, a migration that needed to be applied to the live Supabase project. This is different from `decision-log.md`: that file is *why* a decision was made; this is *what happened* when it hit a real, deployed environment.*

| Date | What happened | Fix |
|---|---|---|
| | | |

## Open feedback backlog

*Feedback received but not yet acted on — so it doesn't quietly disappear between now and the Week 10 walk-through. A simple bullet list is fine; move items here to the tables above once addressed.*
